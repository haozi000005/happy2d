using UnityEngine;
using System.Collections;

public class ItemSelected : MonoBehaviour 
{
   /* private bool isClick;
    private UISprite back;

    void Awake()
    {
        back = transform.FindChild("Background").GetComponent<UISprite>();
    }

    void OnClick()
    {
        back.color = isClick ? new Color(0, 1, 1) : Color.green;
        isClick = !isClick;
    }*/
}
